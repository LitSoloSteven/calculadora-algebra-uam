{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Ejecutar Archivo Actual",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}